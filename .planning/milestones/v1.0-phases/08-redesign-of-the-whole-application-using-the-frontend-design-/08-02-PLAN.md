---
phase: 08-redesign-of-the-whole-application-using-the-frontend-design-
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - resources/js/components/SeverityBadge.vue
  - resources/js/components/CameraStatusDot.vue
  - resources/js/components/SyncStatusDot.vue
  - resources/js/components/ui/table/TableHead.vue
  - resources/js/components/ui/table/TableRow.vue
  - resources/js/components/ui/button/index.ts
  - resources/js/components/ui/badge/index.ts
  - resources/js/components/ui/dialog/DialogOverlay.vue
  - resources/js/components/ui/card/Card.vue
  - resources/js/components/StatusBar.vue
  - resources/js/components/ConnectionBanner.vue
  - resources/js/components/Heading.vue
  - resources/js/components/InputError.vue
  - resources/js/components/TextLink.vue
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Critical severity badges have a red glow in dark mode"
    - "Warning severity badges have an amber glow in dark mode"
    - "Info severity badges have a green glow in dark mode"
    - "Online camera status dots glow green in dark mode"
    - "Table headers use dense data grid styling with uppercase, tracking-wider, 12px font"
    - "Table rows have alternating row shading in dark mode"
    - "Destructive buttons have red glow on hover in dark mode"
    - "Dialog overlays use glassmorphism blur"
  artifacts:
    - path: "resources/js/components/SeverityBadge.vue"
      provides: "Severity badge with dark mode glow box-shadow"
      contains: "shadow-[0_0_12px"
    - path: "resources/js/components/ui/table/TableHead.vue"
      provides: "Dense data grid table header"
      contains: "uppercase tracking-wider"
    - path: "resources/js/components/ui/table/TableRow.vue"
      provides: "Alternating row shading"
      contains: "odd:bg-muted/20"
  key_links:
    - from: "resources/js/components/SeverityBadge.vue"
      to: "AlertFeedItem.vue, alerts/Index.vue, events/Index.vue"
      via: "Component import"
      pattern: "SeverityBadge"
    - from: "resources/js/components/ui/table/TableHead.vue"
      to: "EventHistoryTable.vue, cameras/Index.vue, personnel/Index.vue"
      via: "shadcn-vue table component import"
      pattern: "TableHead"
---

<objective>
Component polish: add dark mode glow effects to severity/status indicators, apply dense data grid styling to shadcn-vue table components, enhance button/badge/dialog/card UI components, and polish status indicators and utility components.

Purpose: These component-level modifications apply the ops center visual identity (D-09, D-10, D-11, D-12) at the reusable component level, so every page that uses these components automatically gets the enhanced styling.

Output: Updated severity badges with glow, status dots with glow, dense data grid table components, enhanced button/badge/dialog/card variants, polished StatusBar/ConnectionBanner/Heading/InputError/TextLink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-CONTEXT.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-RESEARCH.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-01-SUMMARY.md

<interfaces>
<!-- Key component interfaces the executor needs -->

SeverityBadge.vue props:
```typescript
type Props = { severity: AlertSeverity };
// AlertSeverity = 'critical' | 'warning' | 'info'
```

CameraStatusDot.vue props:
```typescript
defineProps<{ isOnline: boolean }>();
```

SyncStatusDot.vue props:
```typescript
defineProps<{
    status: 'synced' | 'pending' | 'failed' | 'not-synced';
    labels?: StatusLabels;
}>();
```

TableHead.vue: uses `cn()` utility with `data-slot="table-head"`, accepts `class` prop
TableRow.vue: uses `cn()` utility with `data-slot="table-row"`, accepts `class` prop
Button index.ts: uses `cva()` with variant/size options, exports `buttonVariants` and `ButtonVariants`
Badge index.ts: uses `cva()` with variant options, exports `badgeVariants` and `BadgeVariants`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glow effects to severity and status indicator components</name>
  <files>resources/js/components/SeverityBadge.vue, resources/js/components/CameraStatusDot.vue, resources/js/components/SyncStatusDot.vue</files>
  <read_first>
    - resources/js/components/SeverityBadge.vue
    - resources/js/components/CameraStatusDot.vue
    - resources/js/components/SyncStatusDot.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**SeverityBadge.vue (per D-09):**

Update the computed `classes` to add dark mode glow box-shadow and slightly transparent background:
```typescript
case 'critical':
    return 'bg-red-500/90 text-white dark:shadow-[0_0_12px_rgba(239,68,68,0.4)]';
case 'warning':
    return 'bg-amber-500/90 text-white dark:shadow-[0_0_10px_rgba(245,158,11,0.35)]';
case 'info':
    return 'bg-emerald-500/90 text-white dark:shadow-[0_0_8px_rgba(16,185,129,0.3)]';
```
The base element classes stay the same: `inline-flex items-center rounded-md px-2 py-0.5 text-xs font-semibold` (change `font-medium` to `font-semibold` per font weight cleanup).

**CameraStatusDot.vue (per D-09):**

Add dark mode glow to the online dot. Change the dot classes:
- Online: `'bg-emerald-500 dark:shadow-[0_0_6px_rgba(16,185,129,0.5)]'`
- Offline: `'bg-neutral-400 dark:bg-neutral-500'` (no glow, unchanged)

**SyncStatusDot.vue (per D-09):**

Add dark mode glow to status dots matching the severity glow pattern:
- synced: `'bg-emerald-500 dark:shadow-[0_0_6px_rgba(16,185,129,0.4)]'`
- pending: `'bg-amber-500 dark:shadow-[0_0_6px_rgba(245,158,11,0.35)]'`
- failed: `'bg-red-500 dark:shadow-[0_0_6px_rgba(239,68,68,0.4)]'`
- not-synced: `'bg-neutral-400 dark:bg-neutral-500'` (no glow, unchanged)
  </action>
  <verify>
    <automated>grep 'shadow-\[0_0_12px' resources/js/components/SeverityBadge.vue && grep 'shadow-\[0_0_6px' resources/js/components/CameraStatusDot.vue && grep 'shadow-\[0_0_6px' resources/js/components/SyncStatusDot.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SeverityBadge.vue critical case contains `dark:shadow-[0_0_12px_rgba(239,68,68,0.4)]`
    - SeverityBadge.vue warning case contains `dark:shadow-[0_0_10px_rgba(245,158,11,0.35)]`
    - SeverityBadge.vue info case contains `dark:shadow-[0_0_8px_rgba(16,185,129,0.3)]`
    - SeverityBadge.vue contains `font-semibold` (not `font-medium`)
    - SeverityBadge.vue critical/warning/info use `/90` opacity backgrounds (e.g., `bg-red-500/90`)
    - CameraStatusDot.vue online dot contains `dark:shadow-[0_0_6px_rgba(16,185,129,0.5)]`
    - CameraStatusDot.vue offline dot does NOT contain `shadow-[`
    - SyncStatusDot.vue synced contains `dark:shadow-[0_0_6px_rgba(16,185,129,0.4)]`
    - SyncStatusDot.vue pending contains `dark:shadow-[0_0_6px_rgba(245,158,11,0.35)]`
    - SyncStatusDot.vue failed contains `dark:shadow-[0_0_6px_rgba(239,68,68,0.4)]`
  </acceptance_criteria>
  <done>All three indicator components have dark-mode-only glow effects per the UI-SPEC severity glow specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Apply dense data grid styling and enhance shadcn-vue UI components</name>
  <files>resources/js/components/ui/table/TableHead.vue, resources/js/components/ui/table/TableRow.vue, resources/js/components/ui/button/index.ts, resources/js/components/ui/badge/index.ts, resources/js/components/ui/dialog/DialogOverlay.vue, resources/js/components/ui/card/Card.vue</files>
  <read_first>
    - resources/js/components/ui/table/TableHead.vue
    - resources/js/components/ui/table/TableRow.vue
    - resources/js/components/ui/button/index.ts
    - resources/js/components/ui/badge/index.ts
    - resources/js/components/ui/dialog/DialogOverlay.vue
    - resources/js/components/ui/card/Card.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**TableHead.vue (per D-12 dense data grid):**

Replace the cn() class string. Change from:
```
'text-foreground h-10 px-2 text-left align-middle font-medium whitespace-nowrap [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translate-y-[2px]'
```
to:
```
'h-8 px-2 text-left align-middle text-xs font-semibold uppercase tracking-wider text-muted-foreground whitespace-nowrap bg-muted/50 dark:bg-muted/30 [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translate-y-[2px]'
```
Key changes: `h-10` to `h-8`, added `text-xs uppercase tracking-wider`, `font-medium` to `font-semibold`, `text-foreground` to `text-muted-foreground`, added `bg-muted/50 dark:bg-muted/30`.

**TableRow.vue (per D-12 dense data grid):**

Replace the cn() class string. Change from:
```
'hover:bg-muted/50 data-[state=selected]:bg-muted border-b transition-colors'
```
to:
```
'hover:bg-accent/50 data-[state=selected]:bg-muted border-b border-border/50 transition-colors odd:bg-muted/20 dark:odd:bg-muted/20'
```
Key changes: `hover:bg-muted/50` to `hover:bg-accent/50`, added `border-border/50`, added `odd:bg-muted/20 dark:odd:bg-muted/20` for alternating rows.

**Button index.ts (per D-10):**

In the `destructive` variant, add dark mode hover glow. Change from:
```
"bg-destructive text-white hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60"
```
to:
```
"bg-destructive text-white hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60 dark:hover:shadow-[0_0_8px_rgba(239,68,68,0.3)]"
```

Also change `font-medium` in the base cva string to `font-semibold`.

**Badge index.ts (per D-10):**

Change `font-medium` in the base cva string to `font-semibold`.

Also add `transition-[color,box-shadow]` is already present (keep it). This enables future glow animations to transition smoothly.

**DialogOverlay.vue (per D-10 glassmorphism):**

Read the current DialogOverlay.vue file. Add `dark:backdrop-blur-sm` to the overlay's class string to add glassmorphism to modal overlays in dark mode. The existing `bg-black/80` or similar should be adjusted to `bg-black/60 dark:backdrop-blur-sm` for the glassmorphism effect.

**Card.vue (per D-10):**

Read the current Card.vue file. Add subtle border opacity in dark mode: add `dark:border-border/50` to the Card's class string so card borders are softer against the dark background.
  </action>
  <verify>
    <automated>grep 'uppercase tracking-wider' resources/js/components/ui/table/TableHead.vue && grep 'odd:bg-muted/20' resources/js/components/ui/table/TableRow.vue && grep 'dark:hover:shadow' resources/js/components/ui/button/index.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - TableHead.vue contains `h-8` (not `h-10`)
    - TableHead.vue contains `text-xs font-semibold uppercase tracking-wider`
    - TableHead.vue contains `text-muted-foreground` (not `text-foreground`)
    - TableHead.vue contains `bg-muted/50 dark:bg-muted/30`
    - TableHead.vue does NOT contain `font-medium`
    - TableRow.vue contains `odd:bg-muted/20 dark:odd:bg-muted/20`
    - TableRow.vue contains `hover:bg-accent/50` (not `hover:bg-muted/50`)
    - TableRow.vue contains `border-border/50`
    - Button index.ts destructive variant contains `dark:hover:shadow-[0_0_8px_rgba(239,68,68,0.3)]`
    - Button index.ts base cva contains `font-semibold` (not `font-medium`)
    - Badge index.ts base cva contains `font-semibold` (not `font-medium`)
    - DialogOverlay.vue contains `dark:backdrop-blur-sm`
    - Card.vue contains `dark:border-border/50`
  </acceptance_criteria>
  <done>Dense data grid table components applied, destructive button glow added, badge/button font weight updated, dialog glassmorphism and card border opacity added.</done>
</task>

<task type="auto">
  <name>Task 3: Polish StatusBar, ConnectionBanner, Heading, InputError, and TextLink</name>
  <files>resources/js/components/StatusBar.vue, resources/js/components/ConnectionBanner.vue, resources/js/components/Heading.vue, resources/js/components/InputError.vue, resources/js/components/TextLink.vue</files>
  <read_first>
    - resources/js/components/StatusBar.vue
    - resources/js/components/ConnectionBanner.vue
    - resources/js/components/Heading.vue
    - resources/js/components/InputError.vue
    - resources/js/components/TextLink.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**StatusBar.vue (per D-10 glassmorphism, D-12 monospace):**

Add glassmorphism to the StatusBar container: `dark:bg-muted/90 dark:backdrop-blur-sm`. Any queue depth or numeric values should use monospace font: add `font-mono text-xs` class to numeric display elements. Replace any `font-medium` with `font-semibold`.

**ConnectionBanner.vue (per D-10):**

Add a subtle glow border animation for the disconnected state. When the banner is showing (connection lost), add `dark:shadow-[0_0_8px_rgba(239,68,68,0.2)]` to the banner container to create a subtle red glow that draws attention. Replace any `font-medium` with `font-semibold`.

**Heading.vue (per UI-SPEC typography):**

Update to use 20px semibold heading style. Ensure the heading element uses `text-xl font-semibold` (change `font-medium` to `font-semibold` if present). The color should use `text-foreground` (default, which now maps to the slate palette).

**InputError.vue (per D-10):**

Add subtle dark mode glow to error text. If the component shows red error text, add `dark:drop-shadow-[0_0_4px_rgba(239,68,68,0.2)]` for a subtle glow effect that makes validation errors pop against the dark background. This is very subtle -- just enough to draw attention.

**TextLink.vue (per D-10):**

Ensure hover state uses accent color. Add or verify `hover:text-primary` class so links highlight with the accent blue on hover, consistent with the new palette.
  </action>
  <verify>
    <automated>grep 'backdrop-blur' resources/js/components/StatusBar.vue && grep 'font-semibold' resources/js/components/Heading.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - StatusBar.vue contains `dark:backdrop-blur-sm` or `backdrop-blur`
    - StatusBar.vue contains `dark:bg-muted/90` or similar opacity background
    - StatusBar.vue does NOT contain `font-medium`
    - ConnectionBanner.vue contains a glow shadow class (`shadow-[`)
    - Heading.vue contains `font-semibold` (not `font-medium`)
    - InputError.vue contains `dark:drop-shadow` or similar dark mode emphasis
    - TextLink.vue contains `hover:text-primary` or equivalent accent hover
  </acceptance_criteria>
  <done>StatusBar has glassmorphism and monospace numerics, ConnectionBanner has glow effect, Heading uses semibold weight, InputError has dark glow, TextLink has accent hover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are visual class modifications to existing components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-02 | Tampering | CSS classes in Vue templates | accept | All styling uses Tailwind utilities, no dynamic user input in class strings. Cosmetic only. |
</threat_model>

<verification>
1. `grep 'shadow-\[0_0_12px' resources/js/components/SeverityBadge.vue` matches (critical glow)
2. `grep 'uppercase tracking-wider' resources/js/components/ui/table/TableHead.vue` matches
3. `grep 'odd:bg-muted/20' resources/js/components/ui/table/TableRow.vue` matches
4. `grep 'dark:hover:shadow' resources/js/components/ui/button/index.ts` matches
5. `grep -r 'font-medium' resources/js/components/SeverityBadge.vue resources/js/components/Heading.vue resources/js/components/ui/table/TableHead.vue resources/js/components/ui/button/index.ts resources/js/components/ui/badge/index.ts` returns no matches
6. `npm run lint:check` passes
7. `npm run format:check` passes
8. `php artisan test --compact` passes
</verification>

<success_criteria>
- All three severity/status indicator components have dark-mode-only glow effects
- Table components use dense data grid styling (compact height, uppercase headers, alternating rows)
- Button destructive variant has dark mode hover glow
- Dialog overlays use glassmorphism
- Cards have subtle dark mode border opacity
- StatusBar has glassmorphism treatment
- font-medium replaced with font-semibold in all modified components
- All tests and linting pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-02-SUMMARY.md`
</output>
