---
phase: 08-redesign-of-the-whole-application-using-the-frontend-design-
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - resources/js/layouts/AuthLayout.vue
  - resources/js/layouts/auth/AuthCardLayout.vue
  - resources/js/layouts/settings/Layout.vue
  - resources/js/components/AppSidebar.vue
  - resources/js/components/AppSidebarHeader.vue
  - resources/js/components/AppLogo.vue
  - resources/js/components/AppLogoIcon.vue
  - resources/js/components/NavMain.vue
  - resources/js/components/NavUser.vue
  - resources/js/components/NavFooter.vue
  - resources/js/components/DashboardTopNav.vue
autonomous: true
requirements:
  - UI-07
  - UI-08

must_haves:
  truths:
    - "Auth pages display in a centered glassmorphism card layout instead of simple layout"
    - "Sidebar has glassmorphism effect in dark mode"
    - "Sidebar active nav item has left accent border"
    - "AppLogo shows FRAS branding instead of Laravel Starter Kit"
    - "DashboardTopNav has transparent background with blur"
  artifacts:
    - path: "resources/js/layouts/AuthLayout.vue"
      provides: "AuthCardLayout import instead of AuthSimpleLayout"
      contains: "AuthCardLayout"
    - path: "resources/js/layouts/auth/AuthCardLayout.vue"
      provides: "Glassmorphism card for auth pages"
      contains: "backdrop-blur"
    - path: "resources/js/components/AppLogo.vue"
      provides: "FRAS branding text"
      contains: "FRAS"
    - path: "resources/js/components/AppSidebar.vue"
      provides: "Glassmorphism sidebar in dark mode"
      contains: "backdrop-blur"
  key_links:
    - from: "resources/js/layouts/AuthLayout.vue"
      to: "resources/js/layouts/auth/AuthCardLayout.vue"
      via: "Import statement"
      pattern: "import.*AuthCardLayout"
    - from: "resources/js/components/AppSidebar.vue"
      to: "resources/js/layouts/app/AppSidebarLayout.vue"
      via: "Component usage in layout"
      pattern: "AppSidebar"
---

<objective>
Layout rework and branding: switch auth to card layout with glassmorphism, add glassmorphism to sidebar, update FRAS branding, polish navigation components, and enhance DashboardTopNav.

Purpose: Implements the structural layout changes (D-05) and branding updates that transform the application's chrome from generic starter kit to security ops center identity. These layout changes wrap all page content and define the application's visual frame.

Output: AuthCardLayout with glassmorphism, glassmorphism sidebar with accent active indicators, FRAS branding, polished nav components, transparent DashboardTopNav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-CONTEXT.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-RESEARCH.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-01-SUMMARY.md

<interfaces>
<!-- Current AuthLayout.vue: -->
```vue
<script setup lang="ts">
import AuthLayout from '@/layouts/auth/AuthSimpleLayout.vue';
const { title = '', description = '' } = defineProps<{
    title?: string;
    description?: string;
}>();
</script>
```

<!-- AppSidebar.vue uses SidebarProvider, Sidebar, SidebarContent, SidebarFooter, SidebarHeader from '@/components/ui/sidebar' -->
<!-- NavMain.vue receives items prop with icon, label, href, isActive fields -->
<!-- NavUser.vue receives user prop with name, email, avatar fields -->
<!-- AppLogo.vue currently shows "Laravel Starter Kit" text -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch auth layout and add glassmorphism card styling</name>
  <files>resources/js/layouts/AuthLayout.vue, resources/js/layouts/auth/AuthCardLayout.vue</files>
  <read_first>
    - resources/js/layouts/AuthLayout.vue
    - resources/js/layouts/auth/AuthCardLayout.vue
    - resources/js/layouts/auth/AuthSimpleLayout.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**AuthLayout.vue (per D-05, UI-SPEC Auth Layout Decision):**

Change the import from AuthSimpleLayout to AuthCardLayout:
```vue
<script setup lang="ts">
import AuthLayout from '@/layouts/auth/AuthCardLayout.vue';

const { title = '', description = '' } = defineProps<{
    title?: string;
    description?: string;
}>();
</script>
```
The template stays the same (delegates to the imported layout).

**AuthCardLayout.vue (per D-06, D-10 glassmorphism):**

Update the existing AuthCardLayout.vue with the ops portal styling:

1. Change the outer container background from `bg-muted` to `bg-background` so it uses the full dark/light theme background.

2. Change the card max-width from `max-w-md` to `max-w-[400px]` per the UI-SPEC.

3. Add glassmorphism to the Card component: add `border-border/50 dark:bg-card/75 dark:backdrop-blur-lg` to the Card's class.

4. Change the logo icon color from `text-black dark:text-white` to `text-primary` so it uses the accent blue.

5. Remove `font-medium` from the Link class (the logo link).

6. Update CardHeader padding from `px-10` to `px-8` per the UI-SPEC.

7. Update CardContent padding from `px-10` to `px-8` per the UI-SPEC.

The final Card element should look like:
```vue
<Card class="rounded-xl border-border/50 dark:bg-card/75 dark:backdrop-blur-lg">
    <CardHeader class="px-8 pt-8 pb-0 text-center">
        <CardTitle class="text-xl">{{ title }}</CardTitle>
        <CardDescription>{{ description }}</CardDescription>
    </CardHeader>
    <CardContent class="px-8 py-8">
        <slot />
    </CardContent>
</Card>
```
  </action>
  <verify>
    <automated>grep 'AuthCardLayout' resources/js/layouts/AuthLayout.vue && grep 'backdrop-blur-lg' resources/js/layouts/auth/AuthCardLayout.vue && grep 'bg-background' resources/js/layouts/auth/AuthCardLayout.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AuthLayout.vue imports from `'@/layouts/auth/AuthCardLayout.vue'` (not AuthSimpleLayout)
    - AuthLayout.vue does NOT contain `AuthSimpleLayout` anywhere
    - AuthCardLayout.vue outer container uses `bg-background` (not `bg-muted`)
    - AuthCardLayout.vue Card contains `dark:backdrop-blur-lg` and `dark:bg-card/75`
    - AuthCardLayout.vue Card contains `border-border/50`
    - AuthCardLayout.vue Card max-width is `max-w-[400px]` (not `max-w-md`)
    - AuthCardLayout.vue CardHeader uses `px-8` (not `px-10`)
    - AuthCardLayout.vue CardContent uses `px-8` (not `px-10`)
    - AuthCardLayout.vue logo icon uses `text-primary` (not `text-black dark:text-white`)
    - AuthCardLayout.vue does NOT contain `font-medium`
  </acceptance_criteria>
  <done>Auth pages use centered glassmorphism card layout with accent-colored logo, proper padding, and ops portal dark background.</done>
</task>

<task type="auto">
  <name>Task 2: Add glassmorphism sidebar, FRAS branding, and navigation polish</name>
  <files>resources/js/components/AppSidebar.vue, resources/js/components/AppSidebarHeader.vue, resources/js/components/AppLogo.vue, resources/js/components/AppLogoIcon.vue, resources/js/components/NavMain.vue, resources/js/components/NavUser.vue, resources/js/components/NavFooter.vue, resources/js/components/DashboardTopNav.vue, resources/js/layouts/settings/Layout.vue</files>
  <read_first>
    - resources/js/components/AppSidebar.vue
    - resources/js/components/AppSidebarHeader.vue
    - resources/js/components/AppLogo.vue
    - resources/js/components/AppLogoIcon.vue
    - resources/js/components/NavMain.vue
    - resources/js/components/NavUser.vue
    - resources/js/components/NavFooter.vue
    - resources/js/components/DashboardTopNav.vue
    - resources/js/layouts/settings/Layout.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**AppSidebar.vue (per D-10 glassmorphism, UI-SPEC Sidebar Navigation Rework):**

Add glassmorphism classes to the Sidebar component: `dark:bg-sidebar/80 dark:backdrop-blur-xl`. This applies the frosted glass effect to the sidebar in dark mode. Read the file first to determine the exact element to add classes to (likely the `<Sidebar>` component root).

**AppSidebarHeader.vue / AppLogo.vue (per Research Open Question #1, UI-SPEC):**

In AppLogo.vue, change the branding text from `"Laravel Starter Kit"` to `"FRAS"`. Apply steel blue accent: add `text-primary` to the logo icon container background. The container currently has `bg-sidebar-primary text-sidebar-primary-foreground` -- this is fine because the sidebar-primary CSS custom property now maps to the accent blue. Ensure `AppLogoIcon` color class is `text-white` (matches the new `--sidebar-primary-foreground: hsl(0 0% 100%)`). Update `dark:text-black` to `dark:text-white` on the AppLogoIcon since `--sidebar-primary-foreground` is now white in both light and dark modes.

**NavMain.vue (per UI-SPEC Sidebar Navigation Rework):**

Add active item styling: active nav items should have a left accent border. Add to the active item's class: `border-l-[3px] border-primary` and `bg-sidebar-accent font-semibold text-sidebar-accent-foreground`. For hover state: add `hover:bg-sidebar-accent/60` with 150ms transition (`transition-colors duration-150`). Replace any `font-medium` with appropriate weight. Read the file first to understand the current active/hover pattern and modify accordingly.

**NavUser.vue:**

Read the file first. Apply hover state polish: add `transition-colors duration-150` to interactive elements. Replace any `font-medium` with `font-semibold`. Ensure dropdown uses the new palette colors (should inherit from CSS custom property cascade).

**NavFooter.vue:**

Read the file first. Apply consistent hover/transition treatment matching NavMain. Replace any `font-medium` with `font-semibold`.

**DashboardTopNav.vue (per UI-SPEC Tier 1):**

Add transparent background with blur: `bg-transparent dark:bg-background/80 dark:backdrop-blur-sm`. Update the FRAS text/logo to use `text-primary` for the steel blue accent treatment. Replace any `font-medium` with `font-semibold`.

**Settings Layout.vue:**

Read `resources/js/layouts/settings/Layout.vue`. Update the settings sidebar nav to add accent active indicator: active link should use `text-primary font-semibold` and inactive links should use `text-muted-foreground hover:text-foreground`. Replace any `font-medium` with `font-semibold`.
  </action>
  <verify>
    <automated>grep 'FRAS' resources/js/components/AppLogo.vue && grep 'backdrop-blur' resources/js/components/AppSidebar.vue && grep 'border-primary' resources/js/components/NavMain.vue && grep 'text-primary' resources/js/layouts/settings/Layout.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AppLogo.vue contains `FRAS` (not `Laravel Starter Kit`)
    - AppLogo.vue AppLogoIcon uses `dark:text-white` (not `dark:text-black`)
    - AppSidebar.vue Sidebar element contains `dark:bg-sidebar/80` and `dark:backdrop-blur-xl`
    - NavMain.vue active item contains `border-l-[3px] border-primary` or similar left accent
    - NavMain.vue contains `transition-colors` for hover transitions
    - NavMain.vue does NOT contain `font-medium`
    - DashboardTopNav.vue contains `backdrop-blur` for transparent blur effect
    - DashboardTopNav.vue contains `text-primary` for FRAS branding
    - Settings Layout.vue active link uses `text-primary font-semibold`
    - Settings Layout.vue does NOT contain `font-medium`
    - No file in this task contains `font-medium` after changes
  </acceptance_criteria>
  <done>Sidebar has glassmorphism with accent active indicators, FRAS branding applied, all navigation components polished with transitions and proper font weights, DashboardTopNav has transparent blur, settings layout has accent active indicator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Layout and navigation are structural presentation changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03 | Spoofing | AppLogo branding | accept | Branding text change is cosmetic. No security implication from changing "Laravel Starter Kit" to "FRAS". |
</threat_model>

<verification>
1. `grep 'AuthCardLayout' resources/js/layouts/AuthLayout.vue` matches
2. `grep 'backdrop-blur-lg' resources/js/layouts/auth/AuthCardLayout.vue` matches
3. `grep 'FRAS' resources/js/components/AppLogo.vue` matches
4. `grep 'backdrop-blur' resources/js/components/AppSidebar.vue` matches
5. `grep 'text-primary' resources/js/layouts/settings/Layout.vue` matches
6. `grep -r 'font-medium' resources/js/layouts/AuthLayout.vue resources/js/layouts/auth/AuthCardLayout.vue resources/js/components/AppLogo.vue resources/js/components/NavMain.vue resources/js/components/DashboardTopNav.vue resources/js/layouts/settings/Layout.vue` returns no matches
7. `npm run lint:check` passes
8. `npm run format:check` passes
9. `php artisan test --compact` passes
</verification>

<success_criteria>
- Auth pages render in centered glassmorphism card layout
- Sidebar has glassmorphism effect in dark mode with accent-bordered active items
- Logo shows "FRAS" with accent blue treatment
- All nav components have smooth hover transitions
- DashboardTopNav is transparent with blur
- Settings layout has accent active indicator
- No font-medium remaining in any modified file
- All tests and linting pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-03-SUMMARY.md`
</output>
