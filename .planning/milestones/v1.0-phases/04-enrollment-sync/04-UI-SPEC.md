---
phase: 4
slug: enrollment-sync
status: draft
shadcn_initialized: true
preset: new-york-v4
created: 2026-04-10
revised: 2026-04-10
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Enrollment Sync. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn-vue |
| Preset | new-york-v4 (neutral base) |
| Component library | reka-ui (headless primitives) |
| Icon library | lucide-vue-next |
| Font | Instrument Sans |

**Source:** `components.json` (style: new-york-v4, baseColor: neutral, iconLibrary: lucide) and `resources/css/app.css` (verified in codebase).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, gap-1 |
| sm | 8px | Compact element spacing, gap-2, py-2 (enrollment sidebar rows) |
| md | 16px | Default element spacing, gap-4, p-4 (summary panel card padding) |
| lg | 24px | Section padding, space-y-6 |
| xl | 32px | Layout gaps, gap-8 |
| 2xl | 48px | Major section breaks, py-12 (empty states) |
| 3xl | 64px | Page-level spacing |

Exceptions: none

**Source:** Phase 3 UI-SPEC (identical scale). Consistent with existing camera and personnel pages.

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm` |
| Label | 14px | 600 (semibold) | 1.5 | `text-sm font-semibold` |
| Heading | 20px | 600 (semibold) | 1.2 | `text-xl font-semibold tracking-tight` |
| Display | 16px | 600 (semibold) | 1.3 | `text-base font-semibold` (summary panel card titles) |

**Distinct sizes:** 3 -- 14px, 16px, 20px. All page specifications below use ONLY these three sizes. No `text-xs` (12px) or `text-2xl` (24px) anywhere.

**Weights used:** 2 -- regular (400) and semibold (600).

**Source:** Phase 3 UI-SPEC (identical typographic scale). `Heading.vue` uses `text-xl font-semibold tracking-tight`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` hsl(0 0% 100%) / hsl(0 0% 3.9%) dark | Page background, surfaces |
| Secondary (30%) | `var(--card)` hsl(0 0% 100%) / hsl(0 0% 3.9%) dark | Cards, enrollment sidebar, summary panel cards |
| Accent (10%) | `var(--primary)` hsl(0 0% 9%) / hsl(0 0% 98%) dark | "Re-sync All" CTA button, focus rings |
| Destructive | `var(--destructive)` hsl(0 84.2% 60.2%) | Delete actions (unchanged from Phase 3) |
| Muted | `var(--muted-foreground)` hsl(0 0% 45.1%) / hsl(0 0% 63.9%) dark | Secondary text, column headers, timestamps, summary labels |

Accent reserved for:
- "Re-sync All" primary CTA button on personnel Show page
- Focus ring on interactive elements
- Active sidebar nav item (unchanged)

### Semantic Colors for This Phase

| Element | Color | Tailwind Class |
|---------|-------|----------------|
| Sync dot: enrolled | emerald-500 | `bg-emerald-500` |
| Sync dot: pending | amber-500 | `bg-amber-500` |
| Sync dot: failed | red-500 | `bg-red-500` |
| Sync dot: not-synced | neutral-400 / neutral-500 dark | `bg-neutral-400 dark:bg-neutral-500` |
| Sync label: enrolled | emerald-700 / emerald-400 dark | `text-emerald-700 dark:text-emerald-400` |
| Sync label: pending | amber-700 / amber-400 dark | `text-amber-700 dark:text-amber-400` |
| Sync label: failed | red-700 / red-400 dark | `text-red-700 dark:text-red-400` |
| Sync label: not-synced | muted-foreground | `text-muted-foreground` |
| Retry button | outline variant | `<Button variant="outline" size="sm">` |
| Summary card: enrolled count | emerald-600 / emerald-400 dark | `text-emerald-600 dark:text-emerald-400` |
| Summary card: failed count | red-600 / red-400 dark | `text-red-600 dark:text-red-400` |
| Summary card: pending count | amber-600 / amber-400 dark | `text-amber-600 dark:text-amber-400` |
| Error message text | muted-foreground | `text-sm text-muted-foreground` (operator-friendly error below failed status) |

**Source:** `SyncStatusDot.vue` (verified: existing status colors from Phase 3). Summary card count colors follow the same semantic color scheme but use -600 weight for better readability on card surfaces.

---

## Component Inventory

### Existing Components to Reuse

| Component | Location | Usage in Phase 4 |
|-----------|----------|-------------------|
| SyncStatusDot | `components/SyncStatusDot.vue` | Enrollment sidebar per-camera status (upgraded from hardcoded to real data + Echo) |
| Button | `components/ui/button/` | Retry per-camera, Re-sync All CTA, existing Edit/Delete actions |
| Card, CardContent, CardHeader, CardTitle | `components/ui/card/` | Enrollment summary panel cards on Index, enrollment sidebar on Show |
| Spinner | `components/ui/spinner/` | Loading state during retry/re-sync operations |
| Separator | `components/ui/separator/` | Section dividers (unchanged) |
| Tooltip, TooltipContent, TooltipProvider, TooltipTrigger | `components/ui/tooltip/` | Error message tooltip on failed enrollment status |
| Badge | `components/ui/badge/` | Person type badges (unchanged) |
| Avatar, AvatarImage, AvatarFallback | `components/ui/avatar/` | Personnel list thumbnails (unchanged) |
| Dialog (full set) | `components/ui/dialog/` | Delete confirmation (unchanged) |
| Alert, AlertDescription, AlertTitle | `components/ui/alert/` | Inline alert for enrollment errors or warnings |
| Heading | `components/Heading.vue` | Page headings (unchanged) |
| CameraStatusDot | `components/CameraStatusDot.vue` | Camera online/offline indicator in summary panel |

### New Components to Build

| Component | Location | Purpose |
|-----------|----------|---------|
| EnrollmentSummaryPanel | `components/EnrollmentSummaryPanel.vue` | Per-camera enrollment count cards displayed above personnel table on Index page (D-10) |

### Pages to Modify (No New Pages)

| Page | Location | Modification |
|------|----------|-------------|
| Personnel Index | `pages/personnel/Index.vue` | Add EnrollmentSummaryPanel above search bar; wire SyncStatusDot to real enrollment data |
| Personnel Show | `pages/personnel/Show.vue` | Wire enrollment sidebar to real data, add Retry Enrollment button per camera, add Re-sync All button, add Echo listener for EnrollmentStatusChanged, show error messages for failed enrollments, show enrolled_at timestamps |
| Camera Show | `pages/cameras/Show.vue` | Wire enrolled personnel sidebar to real enrollment data (replace placeholder) |

---

## Page Specifications

### Focal Points

| Screen | Focal Point | Rationale |
|--------|-------------|-----------|
| Personnel Index | EnrollmentSummaryPanel cards row | First thing the operator scans to assess system-wide enrollment health across cameras |
| Personnel Show | Enrollment sidebar "Re-sync All" button and per-camera status list | The operator's primary action area for managing a specific person's enrollment state |
| Camera Show | Enrolled Personnel list with status dots | Shows the operator which personnel are synced to this specific camera |

### Personnel Index -- EnrollmentSummaryPanel Addition (`pages/personnel/Index.vue`)

**Layout:** Existing page structure preserved. New EnrollmentSummaryPanel inserted between the header row and the search bar.

**Insertion point:** After the `<div class="flex items-center justify-between">` header row, before the `<Card v-if="...">` empty state or the search Input.

**EnrollmentSummaryPanel structure:**
```
[Camera A: 180/200 enrolled, 5 failed] [Camera B: 195/200 enrolled, 0 failed] [Camera C: ...]
```

- Horizontal scrollable container: `flex gap-4 overflow-x-auto pb-2` (scroll on narrow screens)
- Each camera is a clickable Card: `w-56 shrink-0 cursor-pointer hover:bg-muted/50 transition-colors`
- Card links to camera Show page (navigates via Wayfinder `show(camera)` from `@/routes/cameras`)
- Card padding: `p-4`

**Per-camera summary card contents:**
1. **Header row:** Camera name (text-sm font-semibold text-foreground, truncate) + CameraStatusDot (inline-flex, right-aligned)
   - Layout: `flex items-center justify-between gap-2`
2. **Counts row:** Three stacked count items
   - Enrolled: `<span class="text-xl font-semibold text-emerald-600 dark:text-emerald-400">{enrolled}</span> <span class="text-sm text-muted-foreground">/ {total} enrolled</span>`
   - Failed (only if > 0): `<span class="text-sm font-semibold text-red-600 dark:text-red-400">{failed}</span> <span class="text-sm text-muted-foreground">failed</span>`
   - Pending (only if > 0): `<span class="text-sm font-semibold text-amber-600 dark:text-amber-400">{pending}</span> <span class="text-sm text-muted-foreground">pending</span>`

**Conditional rendering:** Only show the summary panel when at least one camera exists AND at least one personnel exists. If either is zero, the summary panel is hidden (no empty state for the panel itself).

**SyncStatusDot in table:** Replace hardcoded `status="not-synced"` with computed status derived from personnel enrollment data. The status is the "worst" across all cameras: if any camera is `failed`, show `failed`; else if any is `pending`, show `pending`; else if all are `enrolled`, show `synced`; else show `not-synced`.

**Props change:** The `personnel` prop type extends to include enrollment summary:
```typescript
type PersonnelWithSync = Personnel & {
    sync_status: 'synced' | 'pending' | 'failed' | 'not-synced';
};
```

**New prop for summary data:**
```typescript
type CameraEnrollmentSummary = {
    id: number;
    name: string;
    is_online: boolean;
    enrolled_count: number;
    pending_count: number;
    failed_count: number;
    total_count: number;
};
```

### Personnel Show -- Enrollment Sidebar Upgrade (`pages/personnel/Show.vue`)

**Layout:** Existing 3/5 + 2/5 grid preserved. Right panel Card ("Enrollment Status") upgraded with real data and actions.

**Enrollment sidebar card changes:**

1. **CardHeader update:** Add "Re-sync All" button next to title
   - Layout: `<CardHeader class="flex flex-row items-center justify-between space-y-0">`
   - Title: `<CardTitle>Enrollment Status</CardTitle>`
   - Button: `<Button variant="default" size="sm" :disabled="resyncProcessing">Re-sync All</Button>`
   - The button submits a POST to `EnrollmentController.resyncAll(personnel)` via Wayfinder `.form()` or `useHttp`

2. **Per-camera enrollment row structure:**
   ```
   [Camera Name]   [SyncStatusDot]   [Retry Enrollment] (if failed)
   [Error message or enrolled_at timestamp]
   ```
   - Container: `<div class="space-y-1 border-b border-border py-3 last:border-0">`
   - Row 1: `<div class="flex items-center justify-between gap-2">`
     - Left: Camera name `<span class="text-sm text-foreground">{name}</span>`
     - Center: `<SyncStatusDot :status="enrollment.status" />`
     - Right (if failed): `<Button variant="outline" size="sm" :disabled="retryProcessing">Retry Enrollment</Button>`
   - Row 2 (conditional): Additional detail below the main row
     - If enrolled: `<p class="text-sm text-muted-foreground">Enrolled {relativeTime(enrollment.enrolled_at)}</p>`
     - If failed: `<p class="text-sm text-red-600 dark:text-red-400">{enrollment.last_error}</p>`
     - If pending: `<p class="text-sm text-muted-foreground">Syncing...</p>` with optional Spinner (size-3) inline

3. **Echo listener for real-time updates:**
   ```typescript
   useEcho('fras.alerts', '.EnrollmentStatusChanged', (payload) => {
       if (payload.personnel_id === props.personnel.id) {
           // Update the matching camera enrollment row reactively
       }
   });
   ```

4. **Empty state (no cameras):** Unchanged from Phase 3 -- CameraIcon, "No cameras registered", "Register cameras to enable enrollment."

**Props change:** The `cameras` prop type extends to include enrollment data:
```typescript
type CameraWithEnrollment = {
    id: number;
    name: string;
    is_online: boolean;
    enrollment: {
        status: 'enrolled' | 'pending' | 'failed' | 'not-synced';
        enrolled_at: string | null;
        last_error: string | null;
    } | null;
};
```

### Camera Show -- Enrolled Personnel Sidebar (`pages/cameras/Show.vue`)

**Layout:** Existing 3/5 + 2/5 grid preserved. Right panel Card ("Enrolled Personnel") upgraded from placeholder to real data.

**Enrolled personnel card structure:**
1. **CardHeader:** `<CardTitle>Enrolled Personnel</CardTitle>` with count badge: `<Badge variant="secondary">{count}</Badge>`
2. **List of enrolled personnel:**
   - Each row: `<div class="flex items-center justify-between border-b border-border py-2 last:border-0">`
   - Left: `<div class="flex items-center gap-2">` with Avatar (size-6) + name (`text-sm text-foreground`)
   - Right: SyncStatusDot showing enrollment status for this camera
3. **Empty state (no enrollments):** Same pattern as current placeholder but with updated copy: "No personnel enrolled" / "Personnel will appear here after enrollment sync."

**Props change:** Camera Show now receives enrolled personnel data:
```typescript
type EnrolledPerson = {
    id: number;
    name: string;
    photo_url: string | null;
    custom_id: string;
    enrollment_status: 'enrolled' | 'pending' | 'failed';
    enrolled_at: string | null;
};
```

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Show page) | "Re-sync All" |
| Retry button label | "Retry Enrollment" |
| Summary panel: enrolled suffix | "/ {total} enrolled" |
| Summary panel: failed suffix | "failed" |
| Summary panel: pending suffix | "pending" |
| Empty state heading (enrollment sidebar, no cameras) | "No cameras registered" |
| Empty state body (enrollment sidebar, no cameras) | "Register cameras to enable enrollment." |
| Empty state heading (camera Show, no enrollments) | "No personnel enrolled" |
| Empty state body (camera Show, no enrollments) | "Personnel will appear here after enrollment sync." |
| Enrolled timestamp | "Enrolled {relativeTime}" (e.g., "Enrolled 2 min ago") |
| Pending status detail | "Syncing..." |
| Delete dialog warning line | "This person will also be removed from all enrolled cameras." |
| Error: ACK timeout | "Enrollment timed out. The camera did not respond within the expected window. Try again." |
| Error: 463 | "Photo required for first enrollment" |
| Error: 464 | "Camera could not resolve photo host" |
| Error: 465 | "Camera could not download photo" |
| Error: 466 | "Photo URL returned no data" |
| Error: 467 | "Photo too large; re-upload with smaller file" |
| Error: 468 | "No usable face detected in photo" |
| Error: 474 | "Camera storage full; remove old enrollments" |
| Error: 478 | "Person may already be enrolled" |
| Error: generic | "Enrollment failed. Try again or check camera connectivity." |
| Toast: enrollment dispatched | "Enrollment syncing to cameras." |
| Toast: retry dispatched | "Retrying enrollment to {camera_name}." |
| Toast: resync dispatched | "Re-syncing to all cameras." |
| Toast: delete sync | "Personnel deleted and removed from cameras." |
| Sync status: synced label | "Enrolled" |
| Sync status: pending label | "Pending" |
| Sync status: failed label | "Failed" |
| Sync status: not-synced label | "Not synced" |

**Delete dialog copy update:** The existing delete confirmation dialog (from Phase 3) should append a warning line below the standard confirmation text: "This person will also be removed from all enrolled cameras." This informs the operator that the MQTT DeletePersons command will be dispatched to all cameras where the person was enrolled.

**Note on SyncStatusDot label change:** The existing `SyncStatusDot.vue` uses "Synced" for the `synced` status. In Phase 4, the enrollment context uses "Enrolled" instead. The component prop value remains `synced` but the displayed label should be updated to "Enrolled" to match the enrollment domain language. Implementation options: (a) add an optional `label` prop override, or (b) rename the `synced` variant to `enrolled` throughout. Recommendation: add an optional `labels` prop map so the component remains reusable across domains (camera sync vs enrollment sync).

---

## Interaction Patterns

### Retry Failed Enrollment (ENRL-07)
1. Admin sees failed SyncStatusDot with red error message on the personnel Show page enrollment sidebar
2. Clicks "Retry Enrollment" button (variant="outline", size="sm") next to the failed camera row
3. Button shows disabled + Spinner during processing
4. POST request dispatched to `EnrollmentController.retry(personnel, camera)` via Wayfinder
5. Server re-dispatches enrollment job; returns with toast "Retrying enrollment to {camera_name}."
6. SyncStatusDot transitions to "Pending" immediately (optimistic or via Inertia redirect)
7. When ACK arrives, Echo listener updates the row to "Enrolled" (green) or "Failed" (red) with new error

### Re-sync All (ENRL-08)
1. Admin clicks "Re-sync All" button in the enrollment sidebar CardHeader
2. Button shows disabled + Spinner during processing
3. POST request dispatched to `EnrollmentController.resyncAll(personnel)` via Wayfinder
4. Server resets all enrollment statuses to pending, dispatches to all online cameras
5. Returns with toast "Re-syncing to all cameras."
6. All SyncStatusDots transition to "Pending"
7. As ACKs arrive, Echo listener updates each row individually

### Real-Time Enrollment Status (D-09)
1. Personnel Show page establishes Echo listener on `fras.alerts` channel for `.EnrollmentStatusChanged` event
2. When event arrives with matching `personnel_id`, the corresponding camera enrollment row updates reactively
3. SyncStatusDot changes color and label; enrolled_at or last_error updates in place
4. No full page refresh needed

### Delete with Enrollment Propagation (ENRL-09, D-12)
1. Existing delete dialog flow unchanged (Phase 3 pattern)
2. Delete dialog body includes the additional warning line: "This person will also be removed from all enrolled cameras."
3. Server-side: `PersonnelController::destroy` now also dispatches MQTT DeletePersons to all enrolled cameras
4. Fire-and-forget -- no ACK tracking for deletes
5. Toast changes to: "Personnel deleted and removed from cameras."

### Summary Panel Navigation (D-10)
1. Admin sees per-camera summary cards at the top of the personnel Index page
2. Clicking a camera card navigates to that camera's Show page
3. Cards are horizontally scrollable when there are many cameras

---

## Responsive Behavior

| Breakpoint | Behavior |
|------------|----------|
| < 1024px (mobile/tablet) | Show page: single column (info card stacks above enrollment sidebar). Summary panel on Index: horizontal scroll with `overflow-x-auto`. Retry/Re-sync buttons stack naturally. |
| >= 1024px (lg) | Show page: 3/5 + 2/5 grid. Summary panel shows multiple cards in a row. |

**Summary panel scrolling:** On narrow screens, the horizontal card row scrolls. Cards have `w-56 shrink-0` to maintain readable width. A subtle bottom padding (`pb-2`) ensures the scrollbar does not clip card content.

**Source:** Consistent with Phase 2 and Phase 3 responsive patterns (grid-cols-1 -> lg:grid-cols-5).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn-vue official | Button, Card, CardContent, CardHeader, CardTitle, Badge, Avatar (full set), Dialog (full set), Input, Separator, Spinner, Tooltip (full set), Alert, AlertDescription, AlertTitle | not required (official) |
| Third-party | none | not applicable |

All components are from the official shadcn-vue registry. Tooltip and Alert were already installed but not used in prior phases; they are used in Phase 4 for error detail display. No third-party registries used.

---

## Accessibility Notes

- "Retry Enrollment" and "Re-sync All" buttons include `aria-label` attributes when context is ambiguous (e.g., `aria-label="Retry enrollment to Camera A"`)
- SyncStatusDot retains `aria-label` describing the status (from Phase 3)
- Error messages associated with failed enrollment rows are visible text (not tooltip-only) to ensure screen reader access
- Enrolled timestamp uses `<time>` element with `datetime` attribute for machine-readable dates
- Summary panel cards are keyboard-navigable (as `<Link>` wrapping the Card, or `@click` with `role="link" tabindex="0"`)
- Button disabled states include `aria-disabled` (shadcn Button handles this automatically)
- Delete dialog includes the camera-side warning line as visible text, ensuring screen readers announce the full consequence

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
