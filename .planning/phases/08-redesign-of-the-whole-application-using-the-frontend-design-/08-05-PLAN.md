---
phase: 08-redesign-of-the-whole-application-using-the-frontend-design-
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - resources/js/pages/Welcome.vue
  - resources/js/pages/auth/Login.vue
  - resources/js/pages/auth/Register.vue
  - resources/js/pages/auth/ForgotPassword.vue
  - resources/js/pages/auth/ResetPassword.vue
  - resources/js/pages/auth/VerifyEmail.vue
  - resources/js/pages/auth/ConfirmPassword.vue
  - resources/js/pages/auth/TwoFactorChallenge.vue
  - resources/js/pages/alerts/Index.vue
  - resources/js/pages/events/Index.vue
  - resources/js/components/EventHistoryTable.vue
  - resources/js/components/EventHistoryFilters.vue
  - resources/js/components/EventHistoryPagination.vue
  - resources/js/components/AlertDetailModal.vue
  - resources/js/pages/settings/Profile.vue
  - resources/js/pages/settings/Security.vue
  - resources/js/pages/settings/Appearance.vue
  - resources/js/components/DeleteUser.vue
  - resources/js/components/AppearanceTabs.vue
autonomous: false
requirements:
  - UI-12
  - UI-13
  - UI-14
  - UI-15

must_haves:
  truths:
    - "Welcome page is a dark ops portal with FRAS branding and login/register links"
    - "Welcome page does NOT load fonts from rsms.me CDN"
    - "Auth pages render inside glassmorphism card layout"
    - "Alert feed page has severity borders with glow effects"
    - "Event history page uses dense data grid styling"
    - "Settings pages have accent active indicator on sidebar nav"
    - "All 22 pages render without JavaScript errors"
  artifacts:
    - path: "resources/js/pages/Welcome.vue"
      provides: "Dark ops portal with FRAS branding"
      contains: "FRAS"
    - path: "resources/js/pages/events/Index.vue"
      provides: "Event history with dense data grid"
      contains: "text-xs"
    - path: "resources/js/pages/alerts/Index.vue"
      provides: "Alert feed with severity glow effects"
      contains: "border-l-2"
  key_links:
    - from: "resources/js/pages/Welcome.vue"
      to: "@/routes"
      via: "Wayfinder route imports for login/register/dashboard links"
      pattern: "import.*from '@/routes'"
---

<objective>
Welcome page rewrite, auth page polish, alerts/events page styling, settings page polish, and final font-medium cleanup with visual verification checkpoint.

Purpose: Completes the remaining 14 pages (Welcome, 7 auth, alerts, events, 3 settings) and sweeps all remaining font-medium occurrences. The checkpoint at the end verifies the complete visual transformation across all 22 pages.

Output: Dark ops portal Welcome page, polished auth pages (glassmorphism inherited from Plan 03), styled alert/event pages with dense data grids, settings pages with accent indicators, and zero font-medium occurrences remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-CONTEXT.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-RESEARCH.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-01-SUMMARY.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-02-SUMMARY.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Welcome page as dark ops portal and polish auth pages</name>
  <files>resources/js/pages/Welcome.vue, resources/js/pages/auth/Login.vue, resources/js/pages/auth/Register.vue, resources/js/pages/auth/ForgotPassword.vue, resources/js/pages/auth/ResetPassword.vue, resources/js/pages/auth/VerifyEmail.vue, resources/js/pages/auth/ConfirmPassword.vue, resources/js/pages/auth/TwoFactorChallenge.vue</files>
  <read_first>
    - resources/js/pages/Welcome.vue
    - resources/js/pages/auth/Login.vue
    - resources/js/pages/auth/Register.vue
    - resources/js/pages/auth/ForgotPassword.vue
    - resources/js/pages/auth/ResetPassword.vue
    - resources/js/pages/auth/VerifyEmail.vue
    - resources/js/pages/auth/ConfirmPassword.vue
    - resources/js/pages/auth/TwoFactorChallenge.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**Welcome.vue (per D-06 ops portal -- COMPLETE VISUAL REWRITE):**

The Welcome page is currently the default Laravel starter kit page with hardcoded colors, documentation links, and separate rsms.me font loading. Replace the entire template with a dark ops portal design:

1. **Remove rsms.me font loading:** Delete the `<Head>` section's `<link rel="preconnect" href="https://rsms.me/" />` and `<link rel="stylesheet" href="https://rsms.me/inter/inter.css" />`. Keep the `<Head title="Welcome">` wrapper but remove the font links inside it. Inter is now loaded globally from app.blade.php.

2. **Replace the template** with a dark professional portal:
```vue
<template>
    <Head title="Welcome" />
    <div class="flex min-h-svh flex-col items-center justify-center bg-background p-6">
        <div class="flex w-full max-w-sm flex-col items-center gap-8">
            <!-- Logo -->
            <div class="flex flex-col items-center gap-3">
                <AppLogoIcon class="size-12 fill-current text-primary" />
                <h1 class="text-[28px] font-semibold tracking-tight text-foreground">
                    FRAS
                </h1>
                <p class="text-sm text-muted-foreground text-center">
                    Face Recognition Alert System
                </p>
            </div>

            <!-- Navigation -->
            <div class="flex w-full flex-col gap-3">
                <Link
                    v-if="$page.props.auth.user"
                    :href="dashboard()"
                    class="inline-flex h-10 w-full items-center justify-center rounded-md bg-primary text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
                >
                    View Dashboard
                </Link>
                <template v-else>
                    <Link
                        :href="login()"
                        class="inline-flex h-10 w-full items-center justify-center rounded-md bg-primary text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
                    >
                        Log in
                    </Link>
                    <Link
                        v-if="canRegister"
                        :href="register()"
                        class="inline-flex h-10 w-full items-center justify-center rounded-md border border-border text-sm font-semibold text-foreground transition-colors hover:bg-accent"
                    >
                        Register
                    </Link>
                </template>
            </div>
        </div>
    </div>
</template>
```

Keep the existing `<script setup>` imports (`Head`, `Link`, `dashboard`, `login`, `register`) and props. Add `import AppLogoIcon from '@/components/AppLogoIcon.vue';` at the top of the script block. Remove all starter kit content (Laravel logo SVG, documentation links, framework version text, update links, grid layout). The page should have NO layout (returns `null` in app.ts layout resolver -- this is already configured).

3. **Remove ALL hardcoded colors:** The current template has many hardcoded hex colors like `bg-[#FDFDFC]`, `text-[#1b1b18]`, `border-[#19140035]`, `dark:bg-[#0a0a0a]`, etc. None of these should remain. Use only Tailwind theme utilities (`bg-background`, `text-foreground`, `border-border`, etc.).

**Auth pages (Login, Register, ForgotPassword, ResetPassword, VerifyEmail, ConfirmPassword, TwoFactorChallenge):**

These pages inherit the glassmorphism card layout from Plan 03's AuthCardLayout switch. The per-page work is:

1. Replace any `font-medium` with `font-semibold` in all 7 auth page files
2. Ensure submit buttons have full width: `w-full` class
3. Ensure link text uses `text-primary` on hover for accent color
4. Remove any hardcoded color classes and use theme utilities instead
5. For VerifyEmail.vue: check if there's `font-medium` and replace
6. For ForgotPassword.vue: check if there's `font-medium` and replace
7. For Login.vue: check if there's `font-medium` and replace
  </action>
  <verify>
    <automated>grep 'FRAS' resources/js/pages/Welcome.vue && ! grep 'rsms.me' resources/js/pages/Welcome.vue && ! grep -r 'font-medium' resources/js/pages/auth/ resources/js/pages/Welcome.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Welcome.vue does NOT contain `rsms.me` anywhere
    - Welcome.vue does NOT contain any hardcoded hex colors (`bg-[#`, `text-[#`, `border-[#`, `dark:bg-[#`)
    - Welcome.vue contains `FRAS` heading text
    - Welcome.vue contains `Face Recognition Alert System` description
    - Welcome.vue contains `AppLogoIcon` import and usage
    - Welcome.vue uses `bg-background`, `text-foreground`, `text-muted-foreground` (theme utilities)
    - Welcome.vue contains `bg-primary text-primary-foreground` on the login/dashboard button
    - All 7 auth pages do NOT contain `font-medium`
    - Login.vue, Register.vue submit buttons have `w-full` class
  </acceptance_criteria>
  <done>Welcome page is a dark ops portal with FRAS branding, all auth pages are polished for the glassmorphism card layout, no font-medium remaining in any auth or welcome page.</done>
</task>

<task type="auto">
  <name>Task 2: Style alerts, events, settings pages and sweep remaining font-medium</name>
  <files>resources/js/pages/alerts/Index.vue, resources/js/pages/events/Index.vue, resources/js/components/EventHistoryTable.vue, resources/js/components/EventHistoryFilters.vue, resources/js/components/EventHistoryPagination.vue, resources/js/components/AlertDetailModal.vue, resources/js/pages/settings/Profile.vue, resources/js/pages/settings/Security.vue, resources/js/pages/settings/Appearance.vue, resources/js/components/DeleteUser.vue, resources/js/components/AppearanceTabs.vue</files>
  <read_first>
    - resources/js/pages/alerts/Index.vue
    - resources/js/pages/events/Index.vue
    - resources/js/components/EventHistoryTable.vue
    - resources/js/components/EventHistoryFilters.vue
    - resources/js/components/EventHistoryPagination.vue
    - resources/js/components/AlertDetailModal.vue
    - resources/js/pages/settings/Profile.vue
    - resources/js/pages/settings/Security.vue
    - resources/js/pages/settings/Appearance.vue
    - resources/js/components/DeleteUser.vue
    - resources/js/components/AppearanceTabs.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**alerts/Index.vue (per D-02 Tier 2):**

Read the file first. Apply severity border treatment to alert feed items if not already handled inline. Add severity glow effects to the alert list containers. Replace any `font-medium` with `font-semibold`. Ensure the filter pills/buttons use accent styling. The AlertFeedItem component already has glow from Plan 02 and severity borders from Plan 04.

**events/Index.vue (per D-02 Tier 2, D-12 dense data grid):**

Read the file first. The page uses EventHistoryTable, EventHistoryFilters, and EventHistoryPagination components. Ensure the page wrapper uses proper dark mode treatment. Replace any `font-medium` with `font-semibold`.

**EventHistoryTable.vue (per D-12 dense data grid):**

Read the file first. Apply dense data grid styling:
- Table cells should use `text-xs px-2 py-1` for compact density
- Similarity score column: `font-mono text-xs text-right`
- Timestamp column: `font-mono text-xs`
- The table header and row styling inherits from the updated shadcn-vue table components (Plan 02)
- Add `sticky top-0 z-10 bg-card` to the `<TableHeader>` for fixed header
- Face crop thumbnail should be compact (32px or smaller)
- Replace any `font-medium` with `font-semibold`

**EventHistoryFilters.vue:**

Read the file first. Ensure select/input components use the new palette (should inherit automatically). Replace any `font-medium` with `font-semibold`.

**EventHistoryPagination.vue:**

Read the file first. Apply muted styling to pagination controls. Active page should use accent color: `bg-primary text-primary-foreground`. Replace any `font-medium` with `font-semibold`.

**AlertDetailModal.vue (per D-10 glassmorphism):**

Read the file first. Apply dark card treatment: the modal content area should have `dark:bg-card` and severity glow accent (e.g., a top border colored by severity: `border-t-2 border-red-500` for critical). Replace any `font-medium` with `font-semibold`. Data fields in the modal should use `font-mono text-xs` for technical values.

**Settings pages (Profile.vue, Security.vue, Appearance.vue -- per D-02 Tier 4):**

Read all three files. These get standard admin tier treatment:
- Card forms should have `dark:border-border/50` for subtle borders
- Submit buttons inherit accent blue from palette
- Replace any `font-medium` with `font-semibold`
- Ensure section headings use `text-foreground font-semibold`

**DeleteUser.vue:**

Read the file first. Ensure destructive dialog uses the correct pattern per UI-SPEC Copywriting Contract: "Your account and all associated data will be permanently removed. This action cannot be undone." Replace any `font-medium` with `font-semibold`.

**AppearanceTabs.vue:**

Read the file first. Ensure theme toggle tabs use proper palette colors. Replace any `font-medium` with `font-semibold`.

**Final font-medium sweep:** After all page changes, run `grep -r 'font-medium' resources/js/` to find ANY remaining occurrences. Fix any found in:
- `resources/js/components/ui/input/Input.vue` -- change to `font-normal` (input text should be regular weight)
- `resources/js/components/ui/label/Label.vue` -- change to `font-semibold`
- `resources/js/components/ui/alert/AlertTitle.vue` -- change to `font-semibold`
- `resources/js/components/ui/sidebar/SidebarMenuBadge.vue` -- change to `font-semibold`
- `resources/js/components/ui/sidebar/SidebarGroupLabel.vue` -- change to `font-semibold`
- `resources/js/components/ui/table/TableFooter.vue` -- change to `font-semibold`
- `resources/js/components/ui/dropdown-menu/DropdownMenuLabel.vue` -- change to `font-semibold`
- `resources/js/components/AppHeader.vue` -- change to `font-semibold`
- `resources/js/components/UserInfo.vue` -- change to `font-semibold`
- `resources/js/layouts/auth/AuthSimpleLayout.vue` -- change to `font-semibold` (still used as fallback)
- `resources/js/layouts/auth/AuthSplitLayout.vue` -- change to `font-semibold`
- Any other remaining files

The goal is ZERO `font-medium` occurrences in the entire `resources/js/` directory after this task.
  </action>
  <verify>
    <automated>grep 'font-mono' resources/js/components/EventHistoryTable.vue && grep 'sticky' resources/js/components/EventHistoryTable.vue && REMAINING=$(grep -r 'font-medium' resources/js/ 2>/dev/null | wc -l | tr -d ' ') && [ "$REMAINING" -eq "0" ] && echo "PASS" || echo "FAIL: $REMAINING font-medium remaining"</automated>
  </verify>
  <acceptance_criteria>
    - alerts/Index.vue does NOT contain `font-medium`
    - events/Index.vue does NOT contain `font-medium`
    - EventHistoryTable.vue contains `font-mono` on similarity/timestamp columns
    - EventHistoryTable.vue contains `sticky top-0` on table header
    - EventHistoryTable.vue cells use `text-xs` for compact density
    - AlertDetailModal.vue contains severity-colored top border (`border-t-2`)
    - AlertDetailModal.vue technical values use `font-mono text-xs`
    - All 3 settings pages do NOT contain `font-medium`
    - DeleteUser.vue does NOT contain `font-medium`
    - `grep -r 'font-medium' resources/js/` returns ZERO matches across the entire directory
    - All shadcn-vue UI components that had `font-medium` now use `font-semibold` (or `font-normal` for Input.vue)
  </acceptance_criteria>
  <done>Alerts and events pages have Tier 2 styling with dense data grids and severity effects. Settings pages have Tier 4 treatment. Zero font-medium occurrences remain in the entire resources/js/ directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification -- complete redesign across all pages</name>
  <files>resources/js/pages/Welcome.vue</files>
  <action>
Human visually verifies the complete redesign. No automated changes in this task -- it is a visual verification checkpoint.

What was built across Plans 01-05:
- Slate/steel blue palette (dark-first) across all pages
- Inter font replacing Instrument Sans
- Glassmorphism on sidebar, auth card, dashboard panels, dialog overlays
- Glow effects on severity badges, camera status dots, sync status dots
- Dense data grid tables on cameras, personnel, events pages
- FRAS branding replacing "Laravel Starter Kit"
- Welcome page transformed to dark ops portal
- Auth pages in glassmorphism card layout
- All font-medium replaced with font-semibold/font-normal
- Accent blue primary buttons, focus rings, and active indicators

How to verify:
1. Start the dev server: `composer run dev` (or ensure it's running)
2. Visit the Welcome page (https://fras.test) -- should show dark FRAS portal with logo and login/register buttons
3. Visit Login (https://fras.test/login) -- should show glassmorphism card on dark background
4. Log in and visit Dashboard (https://fras.test/dashboard) -- verify three-panel layout with glassmorphism panels, glow severity badges in alert feed, monospace data in camera rail
5. Toggle dark/light mode in Settings > Appearance -- verify both modes use the new palette
6. Visit Cameras list (https://fras.test/cameras) -- verify dense data grid table with compact rows, uppercase headers
7. Visit Personnel list (https://fras.test/personnel) -- verify dense data grid with sync status glow dots
8. Visit Live Alerts (https://fras.test/alerts) -- verify severity-colored left borders and glow badges
9. Visit Event History (https://fras.test/events) -- verify dense data grid with sticky header, monospace data
10. Check sidebar -- verify glassmorphism effect, FRAS logo, accent blue active indicator
11. Open browser console -- verify NO JavaScript errors on any page

Resume signal: Type "approved" to confirm the redesign looks correct, or describe specific visual issues to fix.
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <done>User has visually verified the complete redesign across all 22 pages and approved the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are visual/structural.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-05 | Tampering | Welcome page content | accept | Welcome page content is static HTML. No user input rendered. |
| T-8-06 | Information Disclosure | Welcome page | accept | Welcome page shows only product name and login/register links. No sensitive information exposed. |
</threat_model>

<verification>
1. `grep 'FRAS' resources/js/pages/Welcome.vue` matches
2. `! grep 'rsms.me' resources/js/pages/Welcome.vue` (no rsms.me references)
3. `grep 'font-mono' resources/js/components/EventHistoryTable.vue` matches
4. `grep -r 'font-medium' resources/js/ | wc -l` returns 0
5. `npm run lint:check` passes
6. `npm run format:check` passes
7. `php artisan test --compact` passes
8. Visual checkpoint approved by user
</verification>

<success_criteria>
- Welcome page is a dark ops portal with FRAS branding (no starter kit content)
- All 7 auth pages render in glassmorphism card layout
- Alert feed page has severity borders and glow effects
- Event history uses dense data grid with sticky header and monospace data
- Settings pages have accent active indicators and clean card forms
- Zero font-medium occurrences in entire resources/js/ directory
- All 22 pages render without JavaScript errors
- Visual checkpoint approved by user
- All automated tests, ESLint, and Prettier pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-05-SUMMARY.md`
</output>
