---
phase: 08-redesign-of-the-whole-application-using-the-frontend-design-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/app.css
  - resources/views/app.blade.php
  - resources/js/app.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Application uses Inter font family instead of Instrument Sans"
    - "Dark mode uses slate/steel blue palette instead of neutral grays"
    - "Light mode uses cool-toned slate palette instead of neutral grays"
    - "Page background on load matches the CSS custom property --background value with no flash"
    - "Inertia progress bar uses accent blue color instead of gray"
  artifacts:
    - path: "resources/css/app.css"
      provides: "Slate/steel blue palette via CSS custom properties and Inter font stack"
      contains: "Inter"
    - path: "resources/views/app.blade.php"
      provides: "Inter font CDN link and HSL background colors"
      contains: "inter:400,600"
    - path: "resources/js/app.ts"
      provides: "Accent blue Inertia progress bar color"
      contains: "#3B82F6"
  key_links:
    - from: "resources/views/app.blade.php"
      to: "resources/css/app.css"
      via: "Inline background HSL values match --background custom property"
      pattern: "hsl\\(222 47% 6%\\)"
---

<objective>
Foundation: swap font to Inter, apply slate/steel blue palette via CSS custom properties, update app.blade.php inline styles, and set accent blue Inertia progress bar.

Purpose: This is the cascade foundation -- updating CSS custom properties in app.css propagates the new palette through ALL shadcn-vue components automatically. Every subsequent plan depends on this palette being in place.

Output: Updated app.css with full slate/steel blue palette for both light and dark modes, updated app.blade.php with Inter font CDN and HSL background values, updated app.ts with accent blue progress bar color.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-CONTEXT.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-RESEARCH.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap font to Inter and update palette CSS custom properties</name>
  <files>resources/css/app.css</files>
  <read_first>
    - resources/css/app.css
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
Update resources/css/app.css with the following changes (per D-07, D-08, D-13):

**1. Font stack swap (D-13):** Replace all `Instrument Sans` references with `Inter`:
- In the `@theme inline` block, change `--font-sans` from `Instrument Sans, ui-sans-serif, ...` to `Inter, ui-sans-serif, system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji'`
- In the `@layer utilities` body/html block, change `'Instrument Sans'` to `'Inter'` in the `--font-sans` property

**2. Light mode palette (`:root` selector) -- replace ALL values with:**
```
--background: hsl(220 20% 97%);
--foreground: hsl(222 47% 11%);
--card: hsl(0 0% 100%);
--card-foreground: hsl(222 47% 11%);
--popover: hsl(0 0% 100%);
--popover-foreground: hsl(222 47% 11%);
--primary: hsl(217 91% 50%);
--primary-foreground: hsl(0 0% 100%);
--secondary: hsl(220 14% 92%);
--secondary-foreground: hsl(222 47% 11%);
--muted: hsl(220 14% 95%);
--muted-foreground: hsl(215 15% 45%);
--accent: hsl(220 14% 92%);
--accent-foreground: hsl(222 47% 11%);
--destructive: hsl(0 84% 60%);
--destructive-foreground: hsl(0 0% 100%);
--border: hsl(220 13% 89%);
--input: hsl(220 13% 89%);
--ring: hsl(217 91% 50%);
```
Keep `--chart-*` values as-is. Keep `--radius: 0.5rem` as-is.

Update light mode sidebar properties:
```
--sidebar-background: hsl(220 20% 97%);
--sidebar-foreground: hsl(222 47% 20%);
--sidebar-primary: hsl(217 91% 50%);
--sidebar-primary-foreground: hsl(0 0% 100%);
--sidebar-accent: hsl(220 14% 93%);
--sidebar-accent-foreground: hsl(222 47% 20%);
--sidebar-border: hsl(220 13% 89%);
--sidebar-ring: hsl(217 91% 50%);
--sidebar: hsl(220 20% 97%);
```

**3. Dark mode palette (`.dark` selector) -- replace ALL values with:**
```
--background: hsl(222 47% 6%);
--foreground: hsl(210 20% 93%);
--card: hsl(222 47% 9%);
--card-foreground: hsl(210 20% 93%);
--popover: hsl(222 47% 9%);
--popover-foreground: hsl(210 20% 93%);
--primary: hsl(217 91% 60%);
--primary-foreground: hsl(0 0% 100%);
--secondary: hsl(222 30% 14%);
--secondary-foreground: hsl(210 20% 93%);
--muted: hsl(222 30% 12%);
--muted-foreground: hsl(215 15% 55%);
--accent: hsl(222 30% 14%);
--accent-foreground: hsl(210 20% 93%);
--destructive: hsl(0 84% 60%);
--destructive-foreground: hsl(0 0% 100%);
--border: hsl(222 20% 16%);
--input: hsl(222 20% 16%);
--ring: hsl(217 91% 60%);
```
Keep `--chart-*` dark mode values as-is.

Update dark mode sidebar properties:
```
--sidebar-background: hsl(222 47% 7%);
--sidebar-foreground: hsl(210 20% 85%);
--sidebar-primary: hsl(217 91% 60%);
--sidebar-primary-foreground: hsl(0 0% 100%);
--sidebar-accent: hsl(222 30% 14%);
--sidebar-accent-foreground: hsl(210 20% 93%);
--sidebar-border: hsl(222 20% 14%);
--sidebar-ring: hsl(217 91% 60%);
--sidebar: hsl(222 47% 7%);
```

**4. Camera marker dark border update:** Change `.dark .camera-marker` border-color from `#171717` to `hsl(222 20% 16%)` to match the new `--border` value. Add a comment `/* matches --border */`.

Do NOT change the `pulse-ring` keyframes, `camera-marker--online`, `camera-marker--offline` colors, or the mapbox popup styles -- those remain unchanged.
  </action>
  <verify>
    <automated>grep -c 'Inter' resources/css/app.css | grep -q '[3-9]' && grep 'hsl(222 47% 6%)' resources/css/app.css && grep 'hsl(217 91% 60%)' resources/css/app.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - resources/css/app.css contains `Inter` in the `--font-sans` property (at least 2 occurrences: @theme inline and @layer utilities)
    - resources/css/app.css does NOT contain `Instrument Sans` anywhere
    - resources/css/app.css `:root` contains `--background: hsl(220 20% 97%)`
    - resources/css/app.css `:root` contains `--primary: hsl(217 91% 50%)`
    - resources/css/app.css `.dark` contains `--background: hsl(222 47% 6%)`
    - resources/css/app.css `.dark` contains `--primary: hsl(217 91% 60%)`
    - resources/css/app.css `.dark` contains `--sidebar: hsl(222 47% 7%)`
    - resources/css/app.css `.dark .camera-marker` contains `hsl(222 20% 16%)`
    - resources/css/app.css still contains `pulse-ring` keyframes (not removed)
    - resources/css/app.css still contains `--radius: 0.5rem` (not removed)
  </acceptance_criteria>
  <done>All CSS custom properties updated to slate/steel blue palette for both light and dark modes. Font stack changed from Instrument Sans to Inter. Camera marker dark border updated to match new palette.</done>
</task>

<task type="auto">
  <name>Task 2: Update app.blade.php font CDN and inline background, plus Inertia progress color</name>
  <files>resources/views/app.blade.php, resources/js/app.ts</files>
  <read_first>
    - resources/views/app.blade.php
    - resources/js/app.ts
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**1. resources/views/app.blade.php (per D-08, D-13):**

Change the Bunny Fonts CDN link from:
```html
<link href="https://fonts.bunny.net/css?family=instrument-sans:400,500,600" rel="stylesheet" />
```
to:
```html
<link href="https://fonts.bunny.net/css?family=inter:400,600" rel="stylesheet" />
```
Note: Only weights 400 and 600 per UI-SPEC. Weight 500 (medium) is intentionally excluded.

Change the inline background style from oklch to hsl:
```html
<style>
    html {
        background-color: hsl(220 20% 97%);
    }

    html.dark {
        background-color: hsl(222 47% 6%);
    }
</style>
```
This replaces the current `oklch(1 0 0)` and `oklch(0.145 0 0)` values.

Do NOT change: the inline dark mode detection script, favicon links, Vite directive, Inertia head, body class, or any other elements.

**2. resources/js/app.ts:**

Change the Inertia progress bar color from `#4B5563` (gray-600) to `#3B82F6` (blue-500, matching the new accent color `hsl(217 91% 60%)`):
```typescript
progress: {
    color: '#3B82F6',
},
```

Do NOT change any other part of app.ts -- leave layout resolver, Echo config, initializeTheme, and initializeFlashToast unchanged.
  </action>
  <verify>
    <automated>grep 'inter:400,600' resources/views/app.blade.php && grep 'hsl(222 47% 6%)' resources/views/app.blade.php && grep '#3B82F6' resources/js/app.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - resources/views/app.blade.php contains `inter:400,600` (not `instrument-sans`)
    - resources/views/app.blade.php does NOT contain `instrument-sans`
    - resources/views/app.blade.php does NOT contain `oklch`
    - resources/views/app.blade.php contains `hsl(220 20% 97%)` for light background
    - resources/views/app.blade.php contains `hsl(222 47% 6%)` for dark background
    - resources/views/app.blade.php still contains the inline dark mode detection script (`const appearance`)
    - resources/js/app.ts contains `color: '#3B82F6'` in the progress config
    - resources/js/app.ts does NOT contain `#4B5563`
    - resources/js/app.ts still contains `DashboardLayout` import (not removed)
    - resources/js/app.ts still contains `configureEcho` call (not removed)
  </acceptance_criteria>
  <done>app.blade.php uses Inter font from Bunny CDN (weights 400,600), inline background uses HSL values matching CSS custom properties, and Inertia progress bar uses accent blue (#3B82F6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this plan. All changes are visual/presentational CSS and HTML template modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | CSS custom properties | accept | CSS custom properties are set in server-rendered HTML; no user input involved. Client-side manipulation is cosmetic only and does not affect data or auth. |
</threat_model>

<verification>
1. `grep -c 'Inter' resources/css/app.css` returns 2 or more
2. `grep -c 'Instrument Sans' resources/css/app.css` returns 0
3. `grep 'inter:400,600' resources/views/app.blade.php` matches
4. `grep '#3B82F6' resources/js/app.ts` matches
5. `npm run lint:check` passes
6. `npm run format:check` passes
7. `php artisan test --compact` passes (existing tests still work with new palette)
</verification>

<success_criteria>
- Font family changed from Instrument Sans to Inter across all CSS and HTML files
- CSS custom properties updated to slate/steel blue palette for both light and dark modes
- app.blade.php inline background uses HSL values matching CSS custom properties (no FOUC)
- Inertia progress bar uses accent blue color
- All existing tests pass (palette is purely visual, should not affect test assertions)
- ESLint and Prettier pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-01-SUMMARY.md`
</output>
