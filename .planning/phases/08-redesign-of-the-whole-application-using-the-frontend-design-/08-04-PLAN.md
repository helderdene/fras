---
phase: 08-redesign-of-the-whole-application-using-the-frontend-design-
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - resources/js/pages/Dashboard.vue
  - resources/js/components/DashboardAlertFeed.vue
  - resources/js/components/CameraRail.vue
  - resources/js/components/CameraRailItem.vue
  - resources/js/components/TodayStats.vue
  - resources/js/components/DashboardMap.vue
  - resources/js/components/AlertFeedItem.vue
  - resources/js/pages/cameras/Index.vue
  - resources/js/pages/cameras/Show.vue
  - resources/js/pages/cameras/Create.vue
  - resources/js/pages/cameras/Edit.vue
  - resources/js/pages/personnel/Index.vue
  - resources/js/pages/personnel/Show.vue
  - resources/js/pages/personnel/Create.vue
  - resources/js/pages/personnel/Edit.vue
  - resources/js/components/PhotoDropzone.vue
  - resources/js/components/EnrollmentSummaryPanel.vue
autonomous: true
requirements:
  - UI-09
  - UI-10
  - UI-11

must_haves:
  truths:
    - "Dashboard panels (CameraRail, AlertFeed) have glassmorphism effect in dark mode"
    - "Dashboard camera rail items use compact 40px row height with monospace counts"
    - "TodayStats numbers use 28px semibold display size"
    - "Camera list page uses dense data grid table styling"
    - "Personnel list page uses dense data grid table styling"
    - "Camera and personnel forms have card-contained layout with 24px section gaps"
  artifacts:
    - path: "resources/js/components/DashboardAlertFeed.vue"
      provides: "Glassmorphism alert feed panel"
      contains: "backdrop-blur"
    - path: "resources/js/components/CameraRail.vue"
      provides: "Glassmorphism camera rail panel"
      contains: "backdrop-blur"
    - path: "resources/js/components/TodayStats.vue"
      provides: "Display-size stat numbers"
      contains: "text-2xl"
    - path: "resources/js/pages/cameras/Index.vue"
      provides: "Dense data grid camera list"
      contains: "text-xs"
  key_links:
    - from: "resources/js/pages/Dashboard.vue"
      to: "resources/js/components/DashboardAlertFeed.vue"
      via: "Component import"
      pattern: "DashboardAlertFeed"
    - from: "resources/js/pages/Dashboard.vue"
      to: "resources/js/components/CameraRail.vue"
      via: "Component import"
      pattern: "CameraRail"
---

<objective>
Dashboard Tier 1 ops center styling and Admin Tier 2 camera/personnel page styling.

Purpose: Transforms the dashboard into a full ops center with glassmorphism panels and dense monitoring data, and applies the professional dark admin styling to camera and personnel CRUD pages. These are the most-used pages and carry the strongest visual impact.

Output: Glassmorphism dashboard panels, compact camera rail, display-size TodayStats, dense data grid camera/personnel lists, card-contained forms with proper spacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-CONTEXT.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-RESEARCH.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-01-SUMMARY.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-02-SUMMARY.md
@.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Tier 1 ops center -- glassmorphism panels and dense dashboard components</name>
  <files>resources/js/pages/Dashboard.vue, resources/js/components/DashboardAlertFeed.vue, resources/js/components/CameraRail.vue, resources/js/components/CameraRailItem.vue, resources/js/components/TodayStats.vue, resources/js/components/DashboardMap.vue, resources/js/components/AlertFeedItem.vue</files>
  <read_first>
    - resources/js/pages/Dashboard.vue
    - resources/js/components/DashboardAlertFeed.vue
    - resources/js/components/CameraRail.vue
    - resources/js/components/CameraRailItem.vue
    - resources/js/components/TodayStats.vue
    - resources/js/components/DashboardMap.vue
    - resources/js/components/AlertFeedItem.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**DashboardAlertFeed.vue (per D-02 Tier 1, D-10 glassmorphism):**

Add glassmorphism to the panel container: `dark:bg-card/70 dark:backdrop-blur-md`. The panel should blend against the dashboard background with a frosted glass effect. Replace any `font-medium` with `font-semibold`. Add compact header styling.

**CameraRail.vue (per D-02 Tier 1, D-10 glassmorphism):**

Add glassmorphism to the rail container: `dark:bg-card/70 dark:backdrop-blur-md`. Replace any `font-medium` with `font-semibold`. Compact the header area.

**CameraRailItem.vue (per D-02 Tier 1, D-12 dense data):**

Apply compact row styling: target 40px row height. Use `h-10 px-2 py-1` for compact padding. Apply monospace font to the recognition count number: `font-mono text-xs`. The camera name should be `text-sm truncate`. Add `transition-colors duration-150 hover:bg-accent/50` for smooth hover. Replace any `font-medium` with `font-semibold`.

**TodayStats.vue (per UI-SPEC Typography Display role):**

Update stat numbers to use display size: `text-2xl font-semibold` (28px equivalent using Tailwind's text-2xl which is 1.5rem/24px -- but since the spec says 28px, use `text-[28px] font-semibold leading-tight`). Add accent color highlight to numbers: `text-primary` for emphasis numbers. Replace any `font-medium` with `font-semibold`. Add subtle card treatment to stat containers if not already present.

**DashboardMap.vue:**

Read the file first. The map component's dark mode border colors should be updated in the JavaScript marker creation code. If the marker creation code hardcodes `#171717` for dark borders, update to match the new palette's border color. The DashboardMap itself likely has minimal visual styling changes since the map tiles handle most of the visual presentation. Replace any `font-medium` with `font-semibold`.

**Dashboard.vue (per D-02 Tier 1):**

Read the file first. This is the orchestrator page for the three-panel layout. Ensure the layout container has proper dark mode background: the page should use `bg-background` (inherited from body). If there are panel gap or padding adjustments needed, apply edge-to-edge treatment per UI-SPEC Tier 1: "No whitespace padding around map -- edge-to-edge." Replace any `font-medium` with `font-semibold`.

**AlertFeedItem.vue (per D-02 Tier 1, D-12 dense data):**

Read the AlertFeedItem.vue. Apply tighter padding and severity border glow in dark mode. Add monospace for similarity scores and timestamps: `font-mono text-xs`. Add severity left border: critical items should have `border-l-2 border-red-500 dark:shadow-[inset_3px_0_6px_-3px_rgba(239,68,68,0.3)]` for a subtle inward glow on the left edge. Warning items: `border-l-2 border-amber-500`. Info items: `border-l-2 border-emerald-500`. Replace any `font-medium` with `font-semibold`.
  </action>
  <verify>
    <automated>grep 'backdrop-blur' resources/js/components/DashboardAlertFeed.vue && grep 'backdrop-blur' resources/js/components/CameraRail.vue && grep 'font-mono' resources/js/components/CameraRailItem.vue && grep 'text-\[28px\]\|text-2xl' resources/js/components/TodayStats.vue && grep 'border-l-2' resources/js/components/AlertFeedItem.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - DashboardAlertFeed.vue contains `dark:backdrop-blur-md` and `dark:bg-card/70`
    - CameraRail.vue contains `dark:backdrop-blur-md` and `dark:bg-card/70`
    - CameraRailItem.vue contains `font-mono text-xs` on count/numeric elements
    - CameraRailItem.vue contains `hover:bg-accent/50` and `transition-colors`
    - TodayStats.vue stat numbers use `text-[28px] font-semibold` or `text-2xl font-semibold`
    - AlertFeedItem.vue contains `border-l-2 border-red-500` or similar severity left border
    - AlertFeedItem.vue contains `font-mono` on similarity/timestamp elements
    - No file in this task contains `font-medium` after changes
    - Dashboard.vue does NOT add extra padding around the map (edge-to-edge)
  </acceptance_criteria>
  <done>Dashboard has full Tier 1 ops center treatment: glassmorphism panels, compact dense data in camera rail, display-size stats, severity-bordered alert items with monospace data.</done>
</task>

<task type="auto">
  <name>Task 2: Camera admin pages -- Tier 2 professional dark styling</name>
  <files>resources/js/pages/cameras/Index.vue, resources/js/pages/cameras/Show.vue, resources/js/pages/cameras/Create.vue, resources/js/pages/cameras/Edit.vue</files>
  <read_first>
    - resources/js/pages/cameras/Index.vue
    - resources/js/pages/cameras/Show.vue
    - resources/js/pages/cameras/Create.vue
    - resources/js/pages/cameras/Edit.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**Camera Index.vue (per D-02 Tier 2, D-12 dense data grid):**

Read the file first. Apply dense data grid styling to the camera list table:
- Table cells should use `text-xs px-2 py-1` for compact density
- Numeric columns (coordinates, device ID) should use `font-mono text-xs`
- Status dots already have glow from Plan 02
- The table inherits dense header/row styling from the updated shadcn-vue table components (Plan 02)
- Ensure the "Add Camera" button uses accent style (it should inherit from `bg-primary` which is now blue)
- Replace any `font-medium` with `font-semibold`

**Camera Show.vue (per D-02 Tier 2):**

Read the file first. This is a split layout (camera info + enrolled personnel sidebar):
- Camera info card should have subtle border: `border-border/50` in dark mode
- Detail fields (device ID, location, coordinates) should use `font-mono text-xs` for data values
- The enrolled personnel sidebar already uses status dots with glow from Plan 02
- Replace any `font-medium` with `font-semibold`

**Camera Create.vue / Edit.vue (per D-02 Tier 2):**

Read the files first. Apply card-contained form styling:
- Form sections should be wrapped in Card components with `rounded-lg` (should inherit from existing pattern)
- Section gaps should be `gap-6` (24px) between form groups for breathing room
- Submit button should use accent style (inherits from `bg-primary` = blue)
- Input fields should pick up the new palette automatically via CSS custom properties
- Replace any `font-medium` with `font-semibold`
  </action>
  <verify>
    <automated>grep 'font-mono' resources/js/pages/cameras/Index.vue && grep 'font-mono' resources/js/pages/cameras/Show.vue && grep 'gap-6' resources/js/pages/cameras/Create.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - cameras/Index.vue table cells use `text-xs` for compact density
    - cameras/Index.vue has `font-mono` on device ID or coordinate columns
    - cameras/Show.vue data values use `font-mono text-xs` for device ID, coordinates
    - cameras/Create.vue and cameras/Edit.vue form section gaps are `gap-6` (24px)
    - No file in this task contains `font-medium` after changes
  </acceptance_criteria>
  <done>All 4 camera pages have Tier 2 professional dark styling with dense data grids, monospace data values, and card-contained forms.</done>
</task>

<task type="auto">
  <name>Task 3: Personnel admin pages and shared components -- Tier 2 professional dark styling</name>
  <files>resources/js/pages/personnel/Index.vue, resources/js/pages/personnel/Show.vue, resources/js/pages/personnel/Create.vue, resources/js/pages/personnel/Edit.vue, resources/js/components/PhotoDropzone.vue, resources/js/components/EnrollmentSummaryPanel.vue</files>
  <read_first>
    - resources/js/pages/personnel/Index.vue
    - resources/js/pages/personnel/Show.vue
    - resources/js/pages/personnel/Create.vue
    - resources/js/pages/personnel/Edit.vue
    - resources/js/components/PhotoDropzone.vue
    - resources/js/components/EnrollmentSummaryPanel.vue
    - .planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-UI-SPEC.md
  </read_first>
  <action>
**Personnel Index.vue (per D-02 Tier 2, D-12 dense data grid):**

Read the file first. Apply dense data grid styling to the personnel list:
- Avatar column: compact size, no extra padding
- Table cells: `text-xs px-2 py-1`
- Custom ID column: `font-mono text-xs`
- Status dots (SyncStatusDot) already have glow from Plan 02
- Replace any `font-medium` with `font-semibold`

**Personnel Show.vue (per D-02 Tier 2):**

Read the file first. Split layout (edit form + enrollment sidebar):
- Apply card treatment to info sections
- Data values should use `font-mono text-xs` where appropriate
- Enrollment sidebar items inherit glow from Plan 02
- Replace any `font-medium` with `font-semibold`

**Personnel Create.vue / Edit.vue (per D-02 Tier 2):**

Read the files first. Apply card-contained form styling:
- Form sections (Photo, Identity, Details, Contact groups) should have `gap-6` between groups
- PhotoDropzone styling should use `border-border` for dark mode dashed border
- Submit button should use accent style
- Replace any `font-medium` with `font-semibold`

**PhotoDropzone.vue and EnrollmentSummaryPanel.vue:**

Read these components. Apply consistent dark mode border styling (`border-border`) and replace any `font-medium` with `font-semibold`. EnrollmentSummaryPanel should get card treatment with `dark:border-border/50`.
  </action>
  <verify>
    <automated>grep 'font-mono' resources/js/pages/personnel/Index.vue && grep 'gap-6' resources/js/pages/personnel/Create.vue && grep 'border-border' resources/js/components/PhotoDropzone.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - personnel/Index.vue table cells use `text-xs` for compact density
    - personnel/Index.vue has `font-mono` on custom ID column
    - personnel/Show.vue has `font-mono` on data values
    - personnel/Create.vue and personnel/Edit.vue form section gaps are `gap-6`
    - PhotoDropzone.vue uses `border-border` for dark mode border
    - EnrollmentSummaryPanel.vue has `dark:border-border/50`
    - No file in this task contains `font-medium` after changes
  </acceptance_criteria>
  <done>All 4 personnel pages and 2 shared components have Tier 2 professional dark styling with dense data grids, monospace data values, card-contained forms, and consistent dark mode treatment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are visual class modifications to existing page templates.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Tampering | Page template classes | accept | No user input in class strings. All changes are static Tailwind utilities. |
</threat_model>

<verification>
1. `grep 'backdrop-blur' resources/js/components/DashboardAlertFeed.vue` matches
2. `grep 'backdrop-blur' resources/js/components/CameraRail.vue` matches
3. `grep 'font-mono' resources/js/components/CameraRailItem.vue` matches
4. `grep 'border-l-2' resources/js/components/AlertFeedItem.vue` matches
5. `grep 'font-mono' resources/js/pages/cameras/Index.vue` matches
6. `grep 'font-mono' resources/js/pages/personnel/Index.vue` matches
7. `grep -r 'font-medium' resources/js/pages/cameras/ resources/js/pages/personnel/ resources/js/components/DashboardAlertFeed.vue resources/js/components/CameraRail.vue resources/js/components/CameraRailItem.vue resources/js/components/TodayStats.vue resources/js/components/AlertFeedItem.vue` returns no matches
8. `npm run lint:check` passes
9. `npm run format:check` passes
10. `php artisan test --compact` passes
</verification>

<success_criteria>
- Dashboard has glassmorphism panels, compact camera rail, display-size stats
- Camera list and personnel list use dense data grid styling
- Camera and personnel forms have card-contained layout with proper spacing
- All data values use monospace font
- All severity/status indicators display glow effects (inherited from Plan 02)
- No font-medium remaining in any modified file
- All tests and linting pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-redesign-of-the-whole-application-using-the-frontend-design-/08-04-SUMMARY.md`
</output>
